version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Node version:"
        - node --version
        - npm --version
        - echo "Current directory $(pwd)"
        - echo "Listing directory contents" && ls -la
        - npm cache clean --force
        - npm install
        - npm install --save lucide-react@0.263.1 react-oidc-context@3.2.0 @babel/plugin-proposal-private-property-in-object jspdf html2canvas
        - npm install -g react-scripts
        - echo "Installing backend dependencies"
        - cd backend && npm install && cd ..
    build:
      commands:
        - echo "Building frontend for testing environment"
        - npm run build:testing
        - echo "Building backend"
        - cd backend
        - mkdir -p dist
        - npm run build
        - cd ..
        - echo "Listing backend dist directory" && ls -la backend/dist
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
      - 'backend/dist/**/*'
  cache:
    paths:
      - node_modules/**/*
      - backend/node_modules/**/*
      - /root/.npm/**/*