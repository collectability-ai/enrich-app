version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing Node 22"
        - n 22  # Uses 'n' Node version manager to install and use Node 22
        - node --version  # Verify Node version
        - npm --version  # Verify npm version
        - echo "Current directory $(pwd)"
        - echo "Listing directory contents" && ls -la
        - npm ci
        - npm install -g react-scripts
        - echo "Installing backend dependencies"
        - cd backend && npm ci && cd ..
    build:
      commands:
        - echo "Building frontend for testing environment"
        - npm run build:testing  # Use the testing-specific build script
        - echo "Building backend"
        - cd backend
        - mkdir -p dist
        - npm run build
        - cd ..
        - echo "Listing backend dist directory" && ls -la backend/dist
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
      - 'backend/dist/**/*'
  cache:
    paths:
      - node_modules/**/*
      - backend/node_modules/**/*
      - /root/.npm/**/*